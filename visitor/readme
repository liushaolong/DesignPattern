Visitor Pattern 访问器模式
模式定义：
   表示一个作用于某对象结构中的各元素的操作。使得可以砂改变（稳定）各元素的类的前提下定义（扩展）作用于这些元素的新操作（变化）。
 要点总结：
  Visitor模式通过所谓双重颁发（double dispatch）来实现在不更改（不添加新操作-编译时）Element类层次结构的前提下，在运行时透明地为类层次结构上的各个类动态添加新的操作（支持变化 ）。
  所谓双重颁发即Visitor模式中间包括了两个多态颁发（注意其中的多态机制）：第一个execute方法的多态辨析；第二个为visitElementX方法的多态辨析。
  visitor模式的最大缺点在于扩展类层次结构（增添新的element子类），会导致visitor类的改变。因此visitor模式适用于”element类层次结构稳定，而其中的操作却经常面临频繁改动“。
